<Window x:Class="RespawnSwitch.App.Overlay.RespawnOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True" Background="Transparent" WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True" ShowActivated="False" SizeToContent="WidthAndHeight">
    <Border Background="#EA10141D" BorderBrush="#3348D7B7" BorderThickness="1" CornerRadius="18" Padding="18,12">
        <Grid MinWidth="300">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="ChampionText" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                    <TextBlock Text="KDA  " Foreground="#8691A6" FontSize="12"/>
                    <TextBlock x:Name="KdaText" Foreground="#C8D0DF" FontSize="12" FontWeight="Medium"/>
                </StackPanel>
                <TextBlock x:Name="OverlayStatusText" Text="本地计时" Foreground="#48D7B7" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock x:Name="CountdownText" Foreground="White" FontSize="34" FontWeight="Bold"/>
                <TextBlock Text="s" Foreground="#8691A6" FontSize="14" Margin="3,12,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
