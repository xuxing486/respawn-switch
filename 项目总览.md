# 复活切换工具 RespawnSwitch

项目日期：2026-08-15  
当前版本：0.2.0  
运行平台：Windows x64  
技术栈：.NET 8、WPF、League Live Client Data API、Windows GSMTC、Win32 窗口管理

## 项目目标

在 League of Legends 英雄阵亡后显示不抢焦点的复活倒计时，并打开抖音桌面客户端；英雄复活后暂停桌面抖音、恢复其窗口状态，再将焦点切回游戏。找不到可信抖音客户端时，可在每次阵亡周期内最多打开一次抖音官网。

## 主要能力

- 读取 League Live Client Data API 判断阵亡、倒计时与复活。
- 透明、无边框、置顶、鼠标穿透、不激活的倒计时悬浮窗。
- 先检查已保存路径、运行进程、注册表和开始菜单，再扫描所有本机固定磁盘。
- 跳过网络盘、移动盘、无权访问目录和目录联接点。
- 根据文件名、产品信息、版本及签名元数据验证 `douyin.exe`。
- 自动、手动和仅网页版三种模式；支持取消扫描和手动选择候选。
- 深色卡片式设置界面、状态卡、进度、候选列表与事件记录。

## 目录

- `src`：产品源码。
- `tests`：自动化测试与桌面集成测试。
- `tools`：测试宿主和诊断工具。
- `build`：发布脚本。
- `docs`：设计、计划、用户需求历史、操作要求与验证记录。
- `outputs`：仅用于本地最终交付，不进入 GitHub。
- `work`：本地缓存、离线依赖和中间文件，不进入 GitHub。

## 构建

```powershell
dotnet restore RespawnSwitch.sln
dotnet build RespawnSwitch.sln -c Release --no-restore
dotnet test RespawnSwitch.sln -c Release --no-build --no-restore
.\build\publish.ps1 -Configuration Release
```

## 真实环境边界

自动化测试、窗口测试、自检和发布包解压复测均已完成。League、抖音和 Windows 窗口行为可能随版本变化，首次使用应在训练模式完成一次实际阵亡/复活验证。网页版只负责打开官网，不控制浏览器内视频暂停。
